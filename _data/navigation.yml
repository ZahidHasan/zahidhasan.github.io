header:
  - titles:
      # @start locale config
      en      : &EN       Archive
      # @end locale config
    url: /archive.html

  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about.html
    
  - titles:
      # @start locale config
      en      : &EN       CV
      # @end locale config
    url: /cv
    
  - titles:
      # @start locale config
      en      : &EN       T
      # @end locale config
    url: /t.html

