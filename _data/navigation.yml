header:
  - titles:
      # @start locale config
      en      : &EN       Archive
      # @end locale config
    url: /archive

  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about
    
  - titles:
      # @start locale config
      en      : &EN       CV
      # @end locale config
    url: /cv
    
  - titles:
      # @start locale config
      en      : &EN       T
      # @end locale config
    url: /t

